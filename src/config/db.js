// src/config/db.js
// Arquivo de configuração de ORM/driver (mantido vazio como placeholder para a estrutura)

export const ORM_OPTIONS = {
    // Exemplo: configurações de pool de conexão, modo estrito, etc.
};
